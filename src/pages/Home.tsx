import React from "react";
import { Header } from "../sectors/Header/Header";

const Home = (prop: { titulo: string }) => {
  return (
    <div>
      <Header></Header>
    </div>
  );
};

export { Home };
